#pragma once

namespace Lumina::UI
{
    // Show the Lumina UI Demo Window
    // Similar to ImGui::ShowDemoWindow(), this demonstrates all ui:: functions
    // Call this every frame to display the demo window
    void ShowDemoWindow(bool* p_open = nullptr);
}
